---
description: Agent 提交 Git 时用双语 Conventional Commits；开发完成后自动提交并推送
alwaysApply: true
---

# Agent 主导的 Git 提交规范

本仓库是 RS Cursor / Auto 主机。开发改动与运行时状态并存；提交说明以开发意图为准。

## 何时提交

- 完成一轮**代码/配置/文档**开发（且按 `AGENTS.md` 该跑的 `npm test` 已通过或已跳过）后，**自动**执行双语 `git commit` 并 `git push` 到当前分支 upstream。
- **不要**再征求 `y` / `c` / `n`，也不要弹出 AskQuestion 问是否提交。
- 用户若明确说「先别提交」「不要 push」「只改不提交」等，则本轮例外，跳过 Git。
- 测试失败：按 `AGENTS.md` 处理，**不要**带着红测提交。

## 推送到哪里

- 默认推送到当前分支的 **upstream**（`git status` / `git rev-parse --abbrev-ref @{upstream}`）。
- 本机常见远程：`rs-cursor` → `https://github.com/RyanYu0118/RS-Cursor-For-Android.git`；若还有 `origin`，不要误推到用户未指定的远程。
- **不要** `git push --force` 到 `main`/`master`，除非用户明确要求。

## 暂存范围

### 1. 核心改动（优先描述）

本会话中有意修改的路径，例如：

- `src/**`、`scripts/**`、`src/web/**`
- `.wiki/**`、`AGENTS.md`、`CLAUDE.md`、`.cursorrules`、`.cursor/rules/**`
- `.claude/skills/**`
- 用户点名要纳入的文件

### 2. 噪声与敏感文件

- **不要**提交敏感文件：`.env`、`auth.json`、`bot-token.txt`、密钥、凭证；若 `git status` 出现应先警告并排除。
- `state/`、`*.log`、`node_modules/` 等已在 `.gitignore` 中；不要强行 `git add -f`。
- 若有意纳入的生成物/产物（例如已发布的 APK），在正文末尾一条带过即可。

推荐命令：

1. `git add` 本会话核心路径
2. 需要工作区干净时再 `git add -A`（尊重 `.gitignore`）
3. 提交前 `git status` 确认无敏感文件、无意外文件
4. 用 HEREDOC 或 UTF-8 临时文件写入**双语** commit message
5. `git push` 到 upstream
6. `git status` 验证

## 提交信息怎么写

1. 标题与正文前几条以**本会话实际完成的开发内容**为准。
2. 严格遵循根目录 `.cursorrules` 的**双语 Conventional Commits**（中文标题在前，英文标题在后；复杂变更附中英列表）。
3. 先写核心逻辑（做了什么、为什么），不要在前几项罗列噪声文件名。

## 推荐流程

1. 该跑测试则 `npm test`；失败则按 `AGENTS.md` 处理，不要带着红测提交。
2. `git status` / `git diff` 核对开发改动。
3. 暂存 → 双语 commit → `git push` → `git status` 验证。
4. 若改动了 `src/`，按 `AGENTS.md` / `auto-restart` 重启主机（`POST /api/restart`）；纯规则/文档/wiki 不必重启。
5. 回复里用一行点出已推送的 commit（短 hash / 分支即可），不必再问是否提交。

## 与 AGENTS.md 的关系

- `AGENTS.md` 的测试与重启要求仍然有效。
- **提交并推送是默认收尾**；以本规则为准，覆盖「先问再提交」的旧习惯。
