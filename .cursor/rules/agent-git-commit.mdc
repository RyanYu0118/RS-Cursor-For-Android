---
description: Agent 提交 Git 时用双语 Conventional Commits；仅在用户选 y/c/n 后提交或推送
alwaysApply: true
---

# Agent 主导的 Git 提交规范

本仓库是 RS Cursor / Auto 主机。开发改动与运行时状态并存；提交说明以开发意图为准。

## 何时提交

- **仅当用户明确同意**才执行 `git commit` / `git push`；不要静默自动 push。
- 完成一轮**代码/配置/文档**开发（且按 `AGENTS.md` 该跑的 `npm test` 已通过或已跳过）后，必须征求用户意见：
  - 优先用 **AskQuestion** 弹出可点击选项（不要只让用户打字）：
    - **提交并推送 (y)**（Recommended）→ `git commit` + `git push`
    - **只提交 (c)** → 仅 `git commit`
    - **暂不提交 (n)** → 跳过
  - 若当前环境没有 AskQuestion，在回复里用同样三项列出，并等待用户选择。
- 用户若已回复，以下**单行简写视为等同点击**（不区分大小写）：
  - `y` / `yes` / `提交并推送` → **提交并推送**
  - `c` / `commit` / `只提交` → 只提交
  - `n` / `no` / `跳过` → 不提交

**说明：** 用户随时发 `y` 即可触发提交并推送，无需打完整句子。

## 推送到哪里

- 默认推送到当前分支的 **upstream**（`git status` / `git rev-parse --abbrev-ref @{upstream}`）。
- 本机常见远程：`rs-cursor` → `https://github.com/RyanYu0118/RS-Cursor-For-Android.git`；若还有 `origin`，不要误推到用户未指定的远程。
- **不要** `git push --force` 到 `main`/`master`，除非用户明确要求。

## 暂存范围

### 1. 核心改动（优先描述）

本会话中有意修改的路径，例如：

- `src/**`、`scripts/**`、`src/web/**`
- `.wiki/**`、`AGENTS.md`、`CLAUDE.md`、`.cursorrules`、`.cursor/rules/**`
- `.claude/skills/**`
- 用户点名要纳入的文件

### 2. 噪声与敏感文件

- **不要**提交敏感文件：`.env`、`auth.json`、`bot-token.txt`、密钥、凭证；若 `git status` 出现应先警告。
- `state/`、`*.log`、`node_modules/` 等已在 `.gitignore` 中；不要强行 `git add -f`。
- 若有意纳入的生成物/产物（例如已发布的 APK），在正文末尾一条带过即可。

推荐命令：

1. `git add` 本会话核心路径
2. 需要工作区干净时再 `git add -A`（尊重 `.gitignore`）
3. 提交前 `git status` 确认无敏感文件、无意外文件
4. 用 HEREDOC 或 UTF-8 临时文件写入**双语** commit message

## 提交信息怎么写

1. 标题与正文前几条以**本会话实际完成的开发内容**为准。
2. 严格遵循根目录 `.cursorrules` 的**双语 Conventional Commits**（中文标题在前，英文标题在后；复杂变更附中英列表）。
3. 先写核心逻辑（做了什么、为什么），不要在前几项罗列噪声文件名。

## 推荐流程

1. 该跑测试则 `npm test`；失败则按 `AGENTS.md` 处理，不要带着红测提交。
2. `git status` / `git diff` 核对开发改动。
3. 征求 `y` / `c` / `n`（AskQuestion 或等价列表）。
4. `y` 或 `c`：暂存 → 双语 commit → `git status` 验证。
5. 仅 `y` 时再 `git push` 到正确 upstream。
6. 若改动了 `src/`，按 `AGENTS.md` / `auto-restart` 重启主机（`POST /api/restart`）；纯规则/文档/wiki 不必重启。

## 与 AGENTS.md 的关系

- `AGENTS.md` 的测试与重启要求仍然有效。
- **提交/推送不再自动执行**；以本规则的 `y` / `c` / `n` 为准。
- 用户规则里「未要求不要提交」与本规则一致：默认先问，用户回 `y`/`c` 再动 Git。
