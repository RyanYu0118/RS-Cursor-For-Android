/**
 * Node entry for how a desktop tool call should appear. The table itself
 * lives with the web client so both projections share one copy.
 */
export {
  activityCopy,
  classifyTool,
  displayLabel,
  durationText,
  changedFiles,
  editCopy,
  editStatsForTurn,
  fileLang,
  fileStats,
  foldTools,
  groupTally,
  isBrowserTool,
  isCreatedPlan,
  isSimpleLs,
  planFields,
  toolBase,
  skillName,
  stepShown,
  thoughtLabel,
  toolOutputText,
  toolPath,
  turnCopy,
  workCopy,
} from '../web/desktop-tool-ui.js';
